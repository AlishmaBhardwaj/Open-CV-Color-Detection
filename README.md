# Open-CV-Color-Detection

Steps : 
1. Download the code and csv file
2. Run all the commands together
3. The system camera will automatically capture an image using the webcam(connected to port 0)
4. The image will apprear on a separate window
5. Click on the desired position to get the color name and RGB values.
